<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.getfitadmin">

    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/getfit"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/getfit_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FragmentContainer"></activity>
        <activity android:name=".RemoveContent" />
        <activity android:name=".User_queries" />
        <activity android:name=".allUsers" />
        <activity android:name=".sendNotfication" />
        <activity
            android:name=".packages"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".equipment"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".fitnessM_items"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".trainer"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".adminHomepage"
            android:theme="@style/AppTheme" />
        <activity android:name=".fitnessitem"
            android:theme="@style/AppTheme"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>